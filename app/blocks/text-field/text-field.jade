mixin text-field(item, inputType)
	- var attributes = {'class': inputType};
	+b('label')&attributes(attributes)
		+e.caption !{item.label}
		+e('div').container
			+e('input').input(type=item.inputType, pattern=item.pattern, placeholder=item.placeholder, required)
